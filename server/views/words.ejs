<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="<%= base %>/style.css">
    <title>Tous les mots</title>
</head>
<body>
    <h1>Tous les mots</h1>
    <ul>
        <% words.forEach(function(word) { %>
            <li><a href="<%= base %>/words/<%= word.id %>"><%= word.text %></a></li>
        <% }); %>
    </ul>
</body>
</html>